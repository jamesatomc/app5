import 'package:flutter/material.dart';

class HomePage401 extends StatelessWidget {
  const HomePage401({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Icon(
          Icons.mail,
          size: 100,
          color: Colors.orange,
        ),
      ),
    );
  }
}